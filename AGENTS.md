# Agent instructions

When asked to run the same-night Police Audit, **read and fully execute** [`POLICE_AUDIT_TONIGHT.md`](./POLICE_AUDIT_TONIGHT.md).

Hard rules: Carbon Docket; cite every rupee; never invent numbers; never overwrite Maharashtra Police amounts; government books only.

## Babuwatch (/babuwatch, /babuwatch/copwatchindia)

Court-record registers built by `babuwatch/engine/build.py` from `babuwatch/profiles/` and `babuwatch/data/`. Read [`babuwatch/README.md`](./babuwatch/README.md) before changing anything there. Hard rules: this repo is public, so commit only name-gated data; never promote a name the naming gate withheld; keep watch-specific text out of the engine; run `npm run test:babuwatch` and `npm run build:babuwatch`; ship through a PR preview.
